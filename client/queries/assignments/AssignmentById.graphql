query AssignmentById($id: String!){
    assignmentById(id: $id) {
        id
        title
        description
        author {
            name
            surname
            username
        }
        xml
        sandbox
    }
}