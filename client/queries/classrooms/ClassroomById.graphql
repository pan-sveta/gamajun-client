query ClassroomById($id: String!){
    classroomById(id: $id) {
        id
        name
        inviteCode
        users {
            name
            surname
            username
            email
        }

    }
}