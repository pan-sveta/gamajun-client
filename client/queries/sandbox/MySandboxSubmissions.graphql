query MySandboxSubmissions($assignmentId:String!) {
    mySandboxSubmissions(assignmentId: $assignmentId) {
        id
        startedAt
        submittedAt
        assignment {
            title
        }
    }
}